---
doctype: dist

header:
  name: Dist-Zilla-MintingProfile-Iller-CSSON

prereqs:
  runtime:
    requires:
      - perl: 5.010001

plugins:
  - +config: Author::CSSON

  - +extend_plugin: ShareDir
    dir: profiles

---
doctype: weaver

plugins:
  - +config: Author::CSSON
